import 'package:flutter/material.dart';

class FreelancerId extends StatelessWidget {
  const FreelancerId({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
